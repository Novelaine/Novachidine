{% extends 'base.html.twig' %}

{% block title %}Contact{% endblock %}

{% block body %}
    <h1>Voir un Contact</h1>
    <div class="trait"></div>
<a href="{{ path('contact_index') }}"><i class="fas fa-undo-alt" id="return"></i></a>


    <a href="{{ path('contact_edit', {'id': contact.id}) }}"><button class="btnModif">Modifier</button></a>

    
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contact.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ contact.nom }}</td>
            </tr>
            <tr>
                <th>Prénom</th>
                <td>{{ contact.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ contact.email }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ contact.message }}</td>
            </tr>
        </tbody>
    </table>
{{ include('contact/_delete_form.html.twig') }}
    
{% endblock %}


